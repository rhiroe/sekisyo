# frozen_string_literal: true

RSpec.describe Sekisyo do
  it "has a version number" do
    expect(Sekisyo::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
